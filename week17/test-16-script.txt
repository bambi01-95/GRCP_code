#!./grcp-16

;; standard definitions are now read from boot.grl
(println 10)
(let ((x 0))
  (while (< x 10)
    (cond
      ( (= x 1) (println 'one)  )
      ( (= x 6) (println 'six)  )
      ( (= x 8) (return 42)    )
      ('t       (println 'other)))
    (++ x)))